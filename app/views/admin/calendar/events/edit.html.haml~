.body
  .mainform
    %h1 Admin Edit Event
    = form_tag admin_calendar_event_path, :method => :put do
      = render :partial => 'event_form', :object => @event 
      %p
        = submit_tag 'Save Changes'
