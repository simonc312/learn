.body
  .mainform
    %h2 Details about #{@event.name}
    %ul#details
      %li
        From:
        = prettifyEventDate(@event.start_at)
      %li
        To:
        = prettifyEventDate(@event.end_at)
      %li
        Location:
        = @event.location

    %h3 Description:
    %p#description= @event.description
    - if isLoggedIn?
      - if isOwner?
        = render :partial => 'delete', :object => @event
    = link_to 'Back to calendar', dashboard_calendar_path
