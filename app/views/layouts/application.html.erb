<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "The Learning Shelter" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Helping Homeless Through Teaching Maker Skills" %>">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>

    <%= csrf_meta_tags %>
  </head>
  <body>
    <header>
      <%= render 'layouts/navbar' %>
    </header>
    <main role="main">
       <%= render 'layouts/messages' %>

       <%= yield %>
    </main>

    <%= javascript_include_tag 'application'%>
  </body>
</html>
